#include <gtest/gtest.h>
#include <bits/stdc++.h>
#include "neural_network.h"

#define FLAG 0

TEST(SimpleTest, HowToUseGoogleTest)
{
    EXPECT_EQ(1, 2 - 1);
    EXPECT_TRUE(true);
    EXPECT_FALSE(false);
}

